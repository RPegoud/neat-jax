defaults:
  - params: base_params
  - network: base_net
  - mutations: mutations
  - env: gymnax/cartpole
  - _self_

  - override hydra/hydra_logging: disabled
  - override hydra/job_logging: disabled

hydra:
  output_subdir: null
  run:
    dir: .
